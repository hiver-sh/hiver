# Snapshot — TypeScript

Persist a sandbox's own filesystem across a full shutdown using a files snapshot, captured on shutdown and restored on the next boot.

Start the gateway first with `hiver up`, then:

```bash
npm install
npm start
```
