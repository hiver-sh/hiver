# Local filesystem mount — TypeScript

Mount a local directory into the sandbox (read-only here) and read files back through the control plane. Useful for local development.

Start the gateway first with `hiver up`, then:

```bash
npm install
npm start
```
