# Claude exec — TypeScript

Run Claude inside the `claude` image and print the buffered result.

Requires `ANTHROPIC_API_KEY` in the environment.

Start the gateway first with `hiver up`, then:

```bash
npm install
export ANTHROPIC_API_KEY=...
npm start
```
