# Apply config — TypeScript

Read the running config, add an egress allow-rule, and apply the update — the server returns the concrete additions/removals in `result.changes`.

Start the gateway first with `hiver up`, then:

```bash
npm install
npm start
```
