# List directory — TypeScript

List the contents of a sandbox mount with `listDirectory` (name, agent-visible path, is-dir, size).

Start the gateway first with `hiver up`, then:

```bash
npm install
npm start
```
