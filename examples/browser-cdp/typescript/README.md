# Browser (CDP) — TypeScript

Drive the sandbox's resident Chromium with Playwright over CDP — attach with `chromium.connectOverCDP()` through the ingress proxy and drive the warm page as if it were local.

Start the gateway first with `hiver up`, then:

```bash
npm install
npm start
```
