# Terminal attach — TypeScript

Attach your local terminal to an interactive shell running inside the sandbox over a TTY exec stream — keystrokes forwarded in raw mode, output streamed back, resizes propagated.

Start the gateway first with `hiver up`, then:

```bash
npm install
npm start
```
