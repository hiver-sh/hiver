# Google Drive filesystem — TypeScript

Mount a Google Drive folder at `/workspace` (interactive OAuth), then stream the sandbox's events. Paste a Desktop-app OAuth client id/secret when prompted.

Start the gateway first with `hiver up`, then:

```bash
npm install
npm start
```
