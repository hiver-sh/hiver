# Resident browser benchmark

Benchmark the resident-browser lifecycle (create → connect → goto → release) across a sweep of QPS levels.

Start the gateway first with `hiver up`, then:

```bash
npm install
npm start
```
