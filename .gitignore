node_modules
**/__pycache__
.venv
.pytest_cache
.env.local
.env.*.local
.DS_Store
bin
dist

# Built TS client packed into the agent-base build context (see the Makefile's
# stage-agent-base-client target); a generated artifact, never committed.
docker/agent-base/hiver-client.tgz